import styled from "styled-components";

export const HeaderContainer = styled.div`
	position: fixed;
	width: 100%;
	top: 0;
	right: 0;
	padding: inherit;
	z-index: 5; /* Top level of the main page */
	height: 58px;
	overflow: hidden;
	padding: 0 6%;
`;
